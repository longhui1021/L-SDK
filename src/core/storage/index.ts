import { localStorageSet, localStorageGet, localStorageRemove } from './localStorage'

import { sessionStorageSet, sessionStorageGet, sessionStorageRemove } from './sessionStorage'

import { setCookie, getCookie, delCookie } from './cookie'

export {
  localStorageSet,
  localStorageGet,
  localStorageRemove,
  sessionStorageSet,
  sessionStorageGet,
  sessionStorageRemove,
  setCookie,
  getCookie,
  delCookie
}
